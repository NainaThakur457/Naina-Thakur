{
  "entities": {
    "ExportedVideo": {
      "title": "Exported Video",
      "description": "Exported video models and script instructions from AI Text-To-Video generator",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the exported file"
        },
        "title": {
          "type": "string",
          "description": "The custom filename of the exported video"
        },
        "prompt": {
          "type": "string",
          "description": "The AI Text-To-Video prompt or script used"
        },
        "videoUrl": {
          "type": "string",
          "description": "The compiled output video asset URL"
        },
        "duration": {
          "type": "string",
          "description": "Calculated playback length of the video"
        },
        "posterUrl": {
          "type": "string",
          "description": "Visual poster representative preview of the video"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp registered on the server"
        },
        "userId": {
          "type": "string",
          "description": "The Google User UUID who owns this export secure record"
        }
      },
      "required": ["id", "title", "prompt", "videoUrl", "userId", "createdAt"]
    }
  },
  "firestore": {
    "exports/{exportId}": {
      "schema": "ExportedVideo",
      "description": "User exported videos generated by our AI Smart Text-to-Video feature"
    }
  }
}
